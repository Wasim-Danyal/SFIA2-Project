- hosts: docker
  become: true
  become_user: jenkins
  roles:
    - docker

- hosts: manager
  become: true
  become_user: jenkins
  vars:
    swarm_info:
  roles:
    - manager

- hosts: manager
  gather_facts: no
  tasks: []

- hosts: worker
  become: true
  become_user: jenkins
  vars:
    swarm_info:
  roles:
    - worker